spring:
  application:
    name: payment
  datasource:
    url: jdbc:mysql://localhost:3306/affablebean
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8090
